% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/st_top_publishers.R
\name{st_publisher_category_breakdown}
\alias{st_publisher_category_breakdown}
\title{Get Publisher Category Breakdown}
\usage{
st_publisher_category_breakdown(
  publisher_ids,
  categories = NULL,
  time_range = "month",
  date = Sys.Date() - 30,
  os = "unified",
  country = "WW",
  auth_token = Sys.getenv("SENSORTOWER_AUTH_TOKEN")
)
}
\arguments{
\item{publisher_ids}{Character vector. Publisher IDs to analyze.}

\item{categories}{Character vector. Category IDs to check. If NULL,
uses major game categories.}

\item{time_range}{Character. Time period: "week", "month", "quarter".
Defaults to "month".}

\item{date}{Date or character. Start date. Defaults to 30 days ago.}

\item{os}{Character. Operating system: "ios", "android", or "unified". Required.}

\item{country}{Character. Country or region code (e.g., "US", "GB", "WW" for worldwide). Required.}

\item{auth_token}{Character. Your Sensor Tower API authentication token.}
}
\value{
A tibble with publisher revenue broken down by category.
}
\description{
Fetches revenue breakdown by category for specified publishers.
This function makes multiple API calls to aggregate publisher performance
across different categories.
}
\examples{
\dontrun{
# Get category breakdown for top publishers
top_pubs <- st_top_publishers(limit = 5, country = "US")
breakdown <- st_publisher_category_breakdown(
  publisher_ids = top_pubs$publisher_id,
  country = "US"  # Match the country
)
}

}
