% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filters.R
\name{st_filter}
\alias{st_filter}
\alias{st_filter_create}
\alias{print.st_filter}
\title{Construct a local filter, then explicitly create it on the server}
\usage{
st_filter(
  genre = NULL,
  monetization = NULL,
  publisher = NULL,
  sdk = NULL,
  custom_fields = NULL,
  filter_id = NULL,
  date_from = NULL,
  date_to = NULL,
  combine = c("and", "or")
)

st_filter_create(filter, auth_token = NULL)

\method{print}{st_filter}(x, ...)
}
\arguments{
\item{genre, publisher, sdk}{Character field values.}

\item{monetization}{One or more of free, iap, ads, subscription.}

\item{custom_fields}{Named list of field values, or a list of explicit field
records with name, values, global and exclude.}

\item{filter_id}{Existing server filter ID(s), mutually exclusive with criteria.}

\item{date_from, date_to}{Optional release-date bounds for Release Date (US).}

\item{combine}{Combine field predicates using and or or.}

\item{filter}{A local st_filter object.}

\item{auth_token}{API token; defaults to SENSORTOWER_AUTH_TOKEN.}

\item{x}{An st_filter object.}

\item{...}{Unused.}
}
\value{
st_filter() returns a local st_filter object without making requests.
st_filter_create() returns the same object with verified server IDs. AND
sends all fields in one request; OR creates one filter per field. Consumers
union OR results by ID. Values inside a field use the provider's semantics.
}
\description{
Construct a local filter, then explicitly create it on the server
}
\examples{
# Construction is local. Only st_filter_create() writes to the server.
st_filter(genre = "RPG", publisher = "Supercell", combine = "and")
st_filter(genre = "RPG", publisher = "Supercell", combine = "or")
}
