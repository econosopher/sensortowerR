% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/discovery.R
\name{st_app}
\alias{st_app}
\title{Retrieve metadata and explicitly convert app IDs}
\usage{
st_app(
  data,
  os = NULL,
  fields = NULL,
  target_os = NULL,
  errors = c("abort", "partial"),
  auth_token = NULL
)
}
\arguments{
\item{data}{A data frame with character \code{app_id} and \code{os} columns, or a
character ID vector. User columns are retained; collisions get \code{.input}.}

\item{os}{Platform for character IDs. Table inputs carry their own platform.}

\item{fields}{Optional names of metadata fields to retain, in addition to IDs
and app_name. Missing fields are returned as missing values.}

\item{target_os}{Optional target ID namespace. Unified conversion uses the
provider's mapping; conversion to a store expands all mapped regional SKUs.}

\item{errors}{Abort by default; partial returns explicit error rows and warns.}

\item{auth_token}{API token; defaults to SENSORTOWER_AUTH_TOKEN.}
}
\value{
A tibble with app metadata and input columns. Conversions include
input_app_id and input_os. No name-based ID matching is performed.
}
\description{
Retrieve metadata and explicitly convert app IDs
}
