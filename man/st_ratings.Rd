% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/specialists.R
\name{st_ratings}
\alias{st_ratings}
\alias{st_reviews}
\title{Fetch rating and review metrics}
\usage{
st_ratings(
  data,
  date_from,
  date_to,
  os = NULL,
  countries = "WW",
  granularity = "daily",
  bundle = c("ratings_incremental", "ratings_cumulative"),
  breakdown = c("app_id", "date"),
  errors = c("abort", "partial"),
  auth_token = NULL
)

st_reviews(
  data,
  date_from,
  date_to,
  os = NULL,
  countries = "WW",
  granularity = "daily",
  breakdown = c("date", "review_rating"),
  languages = NULL,
  review_keywords = NULL,
  review_sentiments = NULL,
  review_tags = NULL,
  search_terms = NULL,
  rating_filters = NULL,
  errors = c("abort", "partial"),
  auth_token = NULL
)
}
\arguments{
\item{data}{A data frame with character \code{app_id} and \code{os} columns, or a
character ID vector. User columns are retained; collisions get \code{.input}.}

\item{date_from, date_to}{Inclusive dates, as Date or YYYY-MM-DD strings.}

\item{os}{Platform for character IDs. Table inputs carry their own platform.}

\item{countries}{Country codes, or WW alone. Defaults to WW.}

\item{granularity}{Sales period: daily, weekly, monthly, quarterly.}

\item{bundle}{ratings_incremental or ratings_cumulative.}

\item{breakdown}{Provider breakdown. Ratings: app_id, app_id/date, region,
region/date, app_version. Reviews: review_rating alone or with date, region,
language or app_version. Each app is requested separately.}

\item{errors}{Abort by default; partial returns explicit error rows and warns.}

\item{auth_token}{API token; defaults to SENSORTOWER_AUTH_TOKEN.}

\item{languages, review_keywords, review_sentiments, review_tags, search_terms}{Optional review filters.}

\item{rating_filters}{Optional star ratings (1 through 5).}
}
\value{
A long tibble retaining breakdown columns. Counts use count, averages
use stars and provider percentages use provider_percentage.
}
\description{
Fetch rating and review metrics
}
